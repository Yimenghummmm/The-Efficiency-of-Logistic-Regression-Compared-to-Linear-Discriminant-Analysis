main_LogReg: run for logistic regression
	
	please note the comment in line 11 -18

	switch the line 20-21 with 22-23 can load difference dataset

	change the code in line 64 from "weight_train = model.fit()" to "weight_train = model.fit_adaptive()", in line 69 from "weight = model.fit()" to "weight = model.fit_adaptive()" can turn on the adaptive stopping criteria  

main_LDA: run for LDA

	please not the comment in line 13-20

	switch the line 22 with 23 can load difference dataset